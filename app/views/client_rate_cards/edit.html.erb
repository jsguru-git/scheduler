<%@page_title = 'Update client rate'%>
<%@second_nav = client_second_nav(@client, 'rate_card')%>

<div class="standard_one_col">
    <div class="cell_1">
        <%=render :partial => 'shared/page_heading'%>
        <%=render :partial => 'edit_rate_card_form', :locals => {:client_rate_card => @client_rate_card, :rate_card => @rate_card, :client => @client}%>
    </div>
</div>

