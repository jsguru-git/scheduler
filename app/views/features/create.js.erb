// Add content to page
$("#feature_list").append("<%= escape_javascript(render(:partial => 'features/display_feature', :locals => {:feature => @feature})) %>");


// Open lightbox
main_lightbox.close();

// Highlight
$('#<%=dom_id(@feature)%>').effect("highlight", {}, 3000);

// Observer new sortable lists
item_sortable.re_apply_sortable();