<% if @project.archived? %>
    <% @page_title = 'New document (Archived)' %>
<% else %>
    <% @page_title = 'New document' %>
<% end %>

<% @second_nav = project_second_nav(@project, 'documents') -%>

<div class="standard_one_col">
    <div class="cell_1">
		<%=render :partial => 'shared/page_heading'%>
		
		<%=render :partial => 'new_upload', :locals => {:document => @document, :project => @project}%>
	</div>
</div>